<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:visibility="gone"
    android:gravity="center"
    app:layout_constraintWidth_min="400dp"
    android:background="#0F000000"
    android:padding="5dp"
    android:id="@+id/rich_link_card">
    <ImageView
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:scaleType="centerCrop"
        android:id="@+id/rich_link_image"
        android:layout_width="110dp"
        android:layout_height="75dp" />
    <TextView
        app:layout_constraintLeft_toRightOf="@id/rich_link_image"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end"
        android:id="@+id/rich_link_title"
        android:textColor="@color/grey_900"
        android:padding="2dp"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="Data"/>

    <TextView
        app:layout_constraintLeft_toLeftOf="@id/rich_link_title"
        app:layout_constraintTop_toBottomOf="@id/rich_link_title"
        app:layout_constraintRight_toRightOf="parent"
        android:maxLines="2"
        android:ellipsize="end"
        android:id="@+id/rich_link_desp"
        android:textColor="@color/blue_grey_300"
        android:padding="2dp"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="Data"/>

    <TextView
        app:layout_constraintLeft_toLeftOf="@id/rich_link_desp"
        app:layout_constraintTop_toBottomOf="@id/rich_link_desp"
        app:layout_constraintRight_toRightOf="parent"
        android:maxLines="1"
        android:ellipsize="end"
        android:singleLine="true"
        android:id="@+id/rich_link_url"
        android:textColor="@color/blue_grey_700"
        android:padding="2dp"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="Data"/>
</androidx.constraintlayout.widget.ConstraintLayout>